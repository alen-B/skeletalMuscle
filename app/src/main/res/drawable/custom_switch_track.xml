<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- switch关闭时导轨样式 灰 -->
    <item android:state_checked="false">
        <shape>
            <!-- 导轨的尺寸、圆角大小、背景颜色、边框颜色和宽度 -->
            <size android:height="26dp" android:width="50dp" />
            <corners android:radius="13dp" />
            <solid android:color="@color/color_cacaca" />
            <stroke android:color="@color/color_cacaca" android:width="1dp" />
        </shape>
    </item>

    <!-- switch打开时的导轨样式 绿 -->
    <item android:state_checked="true">
        <shape>
            <size android:height="26dp" android:width="50dp" />
            <corners android:radius="13dp" />
            <solid android:color="@color/color_blue" />
            <stroke android:color="@color/color_blue" android:width="1dp" />
        </shape>
    </item>

</selector>